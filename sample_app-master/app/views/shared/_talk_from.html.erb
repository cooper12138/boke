<%= form_for(@talk) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "分享你的评论..."%>
  </div>
  <%= f.hidden_field :weibo_id %>

  <%= f.submit "发布", class: "btn btn-primary" %>
<% end %>